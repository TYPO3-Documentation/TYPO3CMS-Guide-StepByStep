# Render Custom Page Fields

Guides in this section show how to display custom TCA fields from the `pages` table in the frontend, using Fluid templates and the `FLUIDTEMPLATE` content object — the standard rendering approach in TYPO3 site packages.

Each guide focuses on rendering a single field type — text, date, image, dropdown — so editors' input in page properties becomes visible on the website.
